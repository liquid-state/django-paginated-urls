============
Installation
============

At the command line::

    $ easy_install django-paginated-urls

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-paginated-urls
    $ pip install django-paginated-urls
